from django.urls import path
from django.urls import re_path

from . import views

app_name = 'blog'
urlpatterns = [
    # path('',views.index,name='index'),
    # re_path('category/(\d)',views.category,name='category'),
    # re_path('detail/(\d)',views.detail,name='detail'),
]